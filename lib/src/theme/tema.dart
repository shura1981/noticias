import 'package:flutter/material.dart';

final miTema= ThemeData.dark().copyWith(
 colorScheme: const ColorScheme.dark(
   primary: Colors.red,
   secondary: Color.fromARGB(255, 5, 159, 255)
  ),
  appBarTheme: const AppBarTheme(
    color: Colors.red
  )
);